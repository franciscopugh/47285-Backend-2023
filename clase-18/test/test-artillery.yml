config:
  target: 'http://localhost:4000/testArtillery'
  phases:
    - duration: 60
      arrivalRate: 5

scenarios:
  - flow:
    - get:
        url: '/'
        headers:
          Authorization: 'Bearer JWT'